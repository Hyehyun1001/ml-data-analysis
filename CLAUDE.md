# Project: ML & Data Analysis Coding

## 1. Code Modification Rules (중요)
- **수정 전 사전 공유:** 코드를 직접 생성하거나 수정하기 전에 **무엇을 어떻게 변경할 것인지 계획을 먼저 간략히 설명**하고 진행한다.
- **원본 데이터 보호:** 원본 데이터셋 파일은 절대 수정하거나 덮어쓰지 않는다.
- **파괴적 변경 방지:** 기존에 동작하던 함수의 시그니처나 파일 구조를 크게 변경해야 할 경우 사전에 명시적으로 안내한다.

## 2. Coding & Style Conventions
- **언어 및 주석:** 
  - 모든 대화 응답과 설명은 **한국어**로 작성한다.
  - 파이썬 코드 내 모든 주석(Docstring, 인라인 주석)은 **명확한 한국어**로 작성한다.
- **코드 품질:** 
  - Pandas, NumPy, Scikit-learn 등의 라이브러리 사용 시 가독성과 벡터화 연산을 우선 고려한다.
  - 전처리 및 분석 로직은 재사용 가능하도록 모듈화된 함수 단위로 작성한다.
  - Matplotlib/Seaborn 차트 생성 시 한글 폰트(AppleGothic) 깨짐 방지 설정을 기본 포함한다.

## 3. Output & Execution
- 터미널에 수천 줄의 대용량 데이터나 로그를 직접 출력하지 않는다.
- 실행 결과는 핵심 지표(행/열 수, 결측치 현황, 주요 통계치, 모델 스코어 등) 위주로 간결하게 요약하여 전달한다.