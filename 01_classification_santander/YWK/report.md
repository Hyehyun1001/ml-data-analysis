# [프로젝트 주제]에 관한 데이터 분석 보고서 템플릿
- 이 문서는 특정 데이터셋에 국한되지 않고, 어떤 주제의 데이터 분석 프로젝트에도 적용할 수 있는 보고서 작성 템플릿이다. 실제 프로젝트에 적용할 때는 `[프로젝트 주제]` 자리를 실제 주제명으로 바꿔 쓴다.
- (예시) 이번 프로젝트는 santander_customer_satisfaction에 관한 프로젝트로, 해당 데이터셋을 이해하고 분석의 전 과정을 직접 설계 및 수행하는 것을 목표로 했다.

## 각 절차 및 수행 과업
01. 데이터 이해 및 EDA
02. 데이터 전처리
03. 모델링 및 비교
04. 하이퍼파라미터 최적화
05. 성능평가 및 해석
06. 결론 및 회고 작성

## 보고서 목차/목록
> 2.3~2.5, 3.5, 3.6: 익명화된 정형(tabular) 데이터 기준 예시 항목 — 데이터 유형이 다르면 맞는 항목으로 교체하거나 생략한다.
> 6장(모델링): 6.1~6.N을 실제 비교한 모델 수만큼 만들고, 각 절에 담당자·튜닝 전후 결과를 담는다.
> 5.3(최적화 기법): 실제 사용한 도구(Hyperopt/Optuna/GridSearchCV 등)로 구체적으로 명시한다.

0. 초록
1. 서론
1.1 연구 배경 및 필요성
1.2 과제 목적
2. 데이터셋 소개
2.1 데이터 출처 및 구성
2.2 데이터 특성 및 구조 분석
2.3 Feature 이름 특징
2.4 동일 값·중복 Feature
2.5 ID 변수
2.6 타깃 변수 분포 분석
3. 탐색적 데이터 분석(EDA)
3.1 기술통계 분석
3.2 상관관계 기반 주요 변수 분석
3.3 이상치 분석
3.4 변수별 이상치 시각화
3.5 중복 값 제거
3.6 특정 컬럼 로그 변환
4. 데이터 전처리
4.1 이상치 처리
4.2 분포 변환
4.3 파생 변수 생성
4.4 피처 정제 및 중요도 기반 가지치기
4.5 스케일링 및 차원 축소
4.6 데이터 분할
5. 분석방법(Analysis method)
5.1 전처리 필요성 및 과정 개요
5.2 사용 모델 설명
5.3 하이퍼파라미터 최적화 기법
5.4 실행환경
5.5 코드 재사용 및 함수화
6. 모델링
6.1 모델 1
6.2 모델 2
6.N 모델 N
7. 결과
7.1 모델별 최종 성능 비교
7.2 최고 성능 모델 분석
7.3 하이퍼파라미터 최적화 효과 분석
7.4 피처 중요도 공통 인사이트
8. 종합 평가
9. 결론
10. 회고
11. 참고문헌
12. 부록:함수코드
12.1 전처리 함수
12.2 공동함수-modeling
12.3 공동함수-하이퍼파라미터 최적화
12.4 공동함수-평가

### 데이터 소개
- 변수의 규모를 describe나 info를 활용해서 통계적인 수치를 표로 넣도록 한다.
- 이때 가능한 특성과 구조를 시각화하여 어떤 데이터들이 있는지를 이해하도록 한다.

### 데이터 전처리
- 데이터 전처리를 정리할 때 각 저장된 이미지를 활용한다.
- 이상치, 결측치, 파생 변수를 생성하는 등 각 기법을 적용하도록 한다.

### 모델링
#### 모델링에서 사용할 모델의 순서
- 실제 프로젝트에서 비교할 모델들을 정한 순서대로 나열한다. (아래는 산탄데르 프로젝트에서 실제로 사용한 예시)
1. Decision Tree
2. GBM
3. XGB
4. LGBM
5. Randomforest
6. Logistic Regression

#### 모델링 성능비교
- 각 모델마다 나온 결과값을 정리하도록 한다. 기본적인 정리는 글로 작성하도록 한다.
- 같은 모델안에 나온 결과값을 표로 비교할 수 있도록 설명글 아래 전처리나 모델 파라미터의 변경점 그리고 그에 따른 정확도 및 평가점수 비교를 표로 만들어 한눈에 볼 수 있도록 한다.
- 하이퍼파라미터로 최적화를 했을 때 나오는 결과값과 그에 따른 베스트파라미터값을 기록하여, 수동으로 파라미터를 조절하여 진행했을 때 결과값과 자동 탐색 기법(Hyperopt, GridSearchCV 등)을 사용했을 때의 차이를 비교할 수 있도록 한다.

## 보고서의 평가 기준
- 데이터 이해도 및 EDA의 깊이
- 전처리 방법의 타당성과 근거 제시
- 모델 비교 논리 와 지표 선정의 적절성
- 최적화 적용 여부 및 성능 개선 정도
- 결과 해석의 명확성과 결론의 완성도

## 출력 형식 규격 (PDF 등 최종 문서로 변환할 때 고정)
- 페이지 여백: A4 기준 상/하 22mm, 좌/우 18mm로 고정한다.
- 본문 폰트: 맑은 고딕(Malgun Gothic)으로 고정한다. 다른 폰트로 임의 대체하지 않는다.
- 코드·수식처럼 고정폭 표기가 꼭 필요한 부분에 한해서만 예외적으로 고정폭 폰트(Consolas 등)를 허용한다.
- 위 여백·폰트 규격은 주제가 바뀌어도(다른 프로젝트에 이 템플릿을 재사용해도) 동일하게 유지한다.
- 폰트 크기 및 스타일
  - 문서 제목(표지): 20pt / Bold
  - 대제목(H2, 예: 1. 서론): 16pt / Bold
  - 중제목(H3, 예: 1.1 연구 배경): 13pt / Bold
  - 소제목(H4, 예: 4.1.1 전처리 7단계 — 이 프로젝트는 3단계 제목까지만 사용해 미해당): 11pt / Bold
  - 본문: 10pt / Regular
  - 표·캡션·각주: 9pt / Regular
  - 코드 블록: 9pt / Regular (고정폭 폰트 유지)

## 서브 에이전트 전략
- 본 보고서를 작성하는데 있어 각 서브에이전트를 사용하여 정확하고 빠르게 진행하는 것을 목표로 한다.
- 빠른 것보다는 정확한 것에 더 초점을 두도록 한다.
- 서브 에이전트는 순차(설명 → 차트 → 검토, 총괄이 전 과정 감독)로 진행한다.

### 총괄 에이전트
- 총괄 에이전트는 각 에이전트에 명령을 내려 정확하게 해당 내용을 수행하는지의 여부와 검토 역할을 진행하도록 한다.
- 설명에 대한 부분과 차트나 그림에 대한 부분을 확인하여 충분한 보충설명이 되는지와 설명에 대한 부분이 명확한지 검토한다.
- 보고서의 순서가 위에 설명된 과업과 작성 순서대로 작업물이 나오는지 확인하도록 한다.

### 설명 에이전트
- 설명 에이전트는 각 문단 안에 문장이 매끄럽게 진행되도록 하며, 문단과 문단 내용이 이어질 수 있도록 작성하고 한번의 검토를 실행하며, 총괄 에이전트에도 한번 검토를 받도록 한다.
- 설명 에이전트는 한 문단의 내용이 끝나면 시각화 할 수 있는 부분들을 차트 에이전트에게 넘겨 그 부분을 근거 자료나 보충설명을 해줄 수 있는 그림이나 차트를 만들 수 있도록 전달한다.
- 검토 에이전트에도 전달하여 내용이 매끄럽게 이어지는지 전체적인 부분에 대해서 검토를 진행하도록 한다.

### 차트 에이전트
- 차트 에이전트는 각 코드 부분에서 시각화를 해볼 수 있는 부분들을 시각화하는 matplotlib를 활용하여 설명에이전트에게서 받은 부분들을 충분히 설명할 수 있게 차트로 만들어두도록 한다.
- 각 차트나 그림은 PNG파일로 저장하며, 어느 부분에 대한 차트인지 파일명으로 간단하게 표시하도록 한다.
- 한 챕터에 많은 그림이나 차트를 사용할 경우 순서대로 파일이 저장될 수 있게 파일명을 지정하도록 한다.
- 그림이나 표를 검토 에이전트에게도 전달하여 확인해볼 수 있도록 한다.

### 검토 에이전트
- 검토 에이전트는 설명 에이전트의 문단이 충분히 설명이 되는지 확인하도록 한다.
- 차트나 그림을 받아서 확인했을 때, 문단과 매끄럽지 않은 부분이 발견되면 다시 각 에이전트에 전달하여 수정할 수 있도록 한다.
- 이미지나 차트에서 깨진 부분이 있는지 확인하도록 한다.

